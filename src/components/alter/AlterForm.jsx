import React from 'react'
import { useParams } from 'react-router-dom'


const AlterForm = () => {

    const {type} = useParams();

    const renderComp = (event) => {
        switch (type) {
            case value:
                
                break;
        
            default:
                break;
        }
    }
  return (
    <div>AlterForm</div>
  )
}

export default AlterForm